# App Layer

This folder contains the FastAPI application code for AI Tech Quest.

`main.py` creates the app, configures CORS, and registers all routers. Feature-specific logic should live in `services/`, while HTTP-specific concerns should stay in `routers/`.

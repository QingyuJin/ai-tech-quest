# Portfolio Feature

Reserved for future portfolio unlock components.

The MVP uses shared `ProjectCard` components. Later this folder can hold unlock animations, case study previews, and contact CTA blocks.

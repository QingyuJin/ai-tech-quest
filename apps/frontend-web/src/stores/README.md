# Stores

Zustand state stores.

Current store:

- Mission completion status.
- Portfolio unlock state.
- Mock FAQ entries.

Future stores can cover user sessions, API cache state, Unity WebGL bridge state, or experiment filters.

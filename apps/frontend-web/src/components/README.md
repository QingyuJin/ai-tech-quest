# Components

Shared components used across pages.

- `common/` keeps generic UI primitives.
- `layout/` keeps app shell and navigation.
- `mission/` keeps reusable mission-specific presentation components.

# RAG Feature

Reserved for RAG-specific UI components.

The first MVP keeps most RAG page logic in `src/pages/RagMissionPage.jsx`; once upload, document selection, retrieval traces, and evaluation views grow, move them into this folder.

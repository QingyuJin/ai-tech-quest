# Features

Feature-specific components live here.

Each mission can grow independently:

- `rag/` for document investigation UI.
- `ml/` for classification and evaluation UI.
- `business/` for FAQ and assistant UI.
- `portfolio/` for unlocked portfolio project cards.

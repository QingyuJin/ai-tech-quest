# Reserved Frontend App Workspace

This folder is reserved for a future migration to a conventional `apps/frontend-web` monorepo layout.

The current active frontend MVP lives in:

```text
00_Core_Interactive_Showcase/frontend_web
```

Use the active path for development, demos, and deployment checks:

```powershell
cd 00_Core_Interactive_Showcase/frontend_web
npm install
npm run dev
```

This reserved folder should not be treated as the source of truth until the migration is intentionally completed.

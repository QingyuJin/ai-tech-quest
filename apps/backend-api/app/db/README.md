# DB

Database adapters and temporary stores.

Current file:

- `mock_store.py` stores FAQ entries in memory.

Future replacements:

- SQLite for local demos.
- Supabase or Postgres for hosted demos.
- Vector database adapter for RAG retrieval.

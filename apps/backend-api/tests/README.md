# Tests

Reserved for backend tests.

Recommended first tests:

- Health endpoint returns status `ok`.
- Mission list contains three missions.
- RAG ask returns citations.
- Business FAQ create persists in mock store.
- ML predict returns accuracy and confusion matrix.
